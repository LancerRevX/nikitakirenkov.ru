<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{% trans %}Tasks{% endtrans %}</title>
        <link rel="stylesheet" href="{{ static("css/dist/styles.css") }}" />
    </head>
    <body>
        <main class="max-w-sm rounded-xl border-2 border-green-600">
            {% for task in tasks %}
                <tasks.Task :task="task"></tasks.Task>
            {% endfor %}
        </main>
    </body>
</html>
